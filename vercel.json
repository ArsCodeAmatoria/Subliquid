{
  "rewrites": [
    { "source": "/(.*)", "destination": "/apps/web/$1" }
  ],
  "buildCommand": "cd apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/.next/standalone",
  "framework": "nextjs",
  "devCommand": "cd apps/web && npm run dev"
} 